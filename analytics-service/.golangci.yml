version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - misspell
    - noctx
formatters:
  enable:
    - goimports

linters-settings:
  govet:
    enable:
      - shadow

issues:
  exclude-rules:
    - path: "_test\\.go"
      linters:
        - errcheck